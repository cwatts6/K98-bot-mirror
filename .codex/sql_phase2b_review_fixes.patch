diff --git a/.github/workflows/sql-validation.yml b/.github/workflows/sql-validation.yml
index e5db6e3..f568c5a 100644
--- a/.github/workflows/sql-validation.yml
+++ b/.github/workflows/sql-validation.yml
@@ -106,7 +106,7 @@ jobs:
           if ($nightly -match '-AllowMain') {
             throw "Nightly export wrapper must not pass -AllowMain."
           }
-          if ($export -notmatch 'if \(\$branch -eq "main" -and -not \$AllowMain\)') {
+          if ($export -notmatch 'if\s*\(\s*\$branch\s+-eq\s+"main"\s+-and\s+-not\s+\$AllowMain\s*\)') {
             throw "Export script no longer contains the normal main-branch protection."
           }
           if ($export -notmatch '-Reason is required when -AllowMain is used') {
diff --git a/deploy/Test-NightlyExportHealth.ps1 b/deploy/Test-NightlyExportHealth.ps1
index 6b9c78b..e130f1c 100644
--- a/deploy/Test-NightlyExportHealth.ps1
+++ b/deploy/Test-NightlyExportHealth.ps1
@@ -3,6 +3,7 @@ param(
     [string]$TaskName = "K98 SQL Nightly Schema Export",
     [int]$MaxExportAgeHours = 30,
     [switch]$SkipScheduledTaskCheck,
+    [int]$LogTailLines = 2000,
     [switch]$WarnOnly
 )
 
@@ -42,35 +43,34 @@ try {
         Add-NightlyExportIssue "Export log not found: $logPath"
     }
     else {
-        $events = Get-Content -Path $logPath |
-            Where-Object { -not [string]::IsNullOrWhiteSpace($_) } |
-            ForEach-Object { ConvertFrom-K98JsonLine -Line $_ } |
-            Where-Object { $null -ne $_ }
-
-        $latestFinish = $events |
-            Where-Object {
-                $_.script -eq "Invoke-NightlyProdSchemaExport.ps1" -and
-                $_.operation -eq "nightly_export_finish"
-            } |
-            Sort-Object { [DateTime]$_.timestamp_utc } -Descending |
-            Select-Object -First 1
-
-        $latestCleanupFailure = $events |
-            Where-Object {
-                $_.script -eq "Invoke-NightlyProdSchemaExport.ps1" -and
-                $_.operation -eq "nightly_export_cleanup" -and
-                $_.status -eq "Failed"
-            } |
-            Sort-Object { [DateTime]$_.timestamp_utc } -Descending |
-            Select-Object -First 1
+        $recentLines = @(Get-Content -Path $logPath -Tail $LogTailLines)
+        for ($index = $recentLines.Count - 1; $index -ge 0; $index--) {
+            if ($null -ne $latestFinish -and $null -ne $latestCleanupFailure) {
+                break
+            }
+            $line = $recentLines[$index]
+            if ([string]::IsNullOrWhiteSpace($line)) {
+                continue
+            }
+
+            $event = ConvertFrom-K98JsonLine -Line $line
+            if ($null -eq $event) {
+                continue
+            }
+
+            if ($null -eq $latestFinish -and
+                $event.script -eq "Invoke-NightlyProdSchemaExport.ps1" -and
+                $event.operation -eq "nightly_export_finish") {
+                $latestFinish = $event
+            }
+
+            if ($null -eq $latestCleanupFailure -and
+                $event.script -eq "Invoke-NightlyProdSchemaExport.ps1" -and
+                $event.operation -eq "nightly_export_cleanup" -and
+                $event.status -eq "Failed") {
+                $latestCleanupFailure = $event
+            }
+        }
 
         if ($null -eq $latestFinish) {
-            Add-NightlyExportIssue "No nightly_export_finish event found in export log."
+            Add-NightlyExportIssue "No nightly_export_finish event found in the last $LogTailLines export log line(s)."
         }
         else {
             $latestFinishUtc = ([DateTime]$latestFinish.timestamp_utc).ToUniversalTime()
@@ -129,6 +129,7 @@ try {
         task_last_run_time = $taskLastRunTime
         task_last_task_result = $taskLastTaskResult
         max_export_age_hours = $MaxExportAgeHours
+        log_tail_lines = $LogTailLines
         latest_export_status = if ($latestFinish) { $latestFinish.status } else { $null }
         latest_export_timestamp_utc = if ($latestFinish) { $latestFinish.timestamp_utc } else { $null }
         issue_count = $issues.Count
diff --git a/deploy/Test-SqlBackupReadiness.ps1 b/deploy/Test-SqlBackupReadiness.ps1
index 7e94ff3..6529c3a 100644
--- a/deploy/Test-SqlBackupReadiness.ps1
+++ b/deploy/Test-SqlBackupReadiness.ps1
@@ -19,13 +19,14 @@ $started = Get-Date
 $issues = New-Object System.Collections.Generic.List[string]
 $warnings = New-Object System.Collections.Generic.List[string]
 
+$script:AppliedBackupPolicyConfigPath = $null
+
 function Get-K98ConfigValue {
     param(
         [Parameter(Mandatory=$true)]$Config,
         [Parameter(Mandatory=$true)][string[]]$Path,
-        [Parameter(Mandatory=$true)]$Default
+        [AllowNull()]$Default
     )
 
     $current = $Config
@@ -57,6 +58,7 @@ function Import-K98BackupPolicyConfig {
     if (-not (Test-Path $Path)) {
         throw "Backup policy config not found: $Path"
     }
+    $script:AppliedBackupPolicyConfigPath = (Resolve-Path $Path).ProviderPath
 
     $config = Get-Content -Raw -Path $Path | ConvertFrom-Json -ErrorAction Stop
     $script:MaxFullBackupAgeHours = [int](Get-K98ConfigValue -Config $config -Path @("backup_policy", "max_full_backup_age_hours") -Default $script:MaxFullBackupAgeHours)
@@ -70,8 +72,6 @@ function Import-K98BackupPolicyConfig {
     }
 }
 
-Import-K98BackupPolicyConfig -Path $ConfigPath
-
 function Get-BackupRowValue {
     param(
         [Parameter(Mandatory=$true)]$Row,
@@ -96,6 +96,8 @@ function Add-BackupWarning {
 }
 
 try {
+    Import-K98BackupPolicyConfig -Path $ConfigPath
+
     $databaseLiteral = ConvertTo-K98SqlLiteral -Value $DatabaseName
     $backupQuery = @"
 SELECT
@@ -188,7 +190,7 @@ WHERE name = $databaseLiteral;
         differential_backup = $diffBackup
         log_backup = $logBackup
         issue_count = $issues.Count
-        backup_policy_config = $ConfigPath
+        backup_policy_config = $script:AppliedBackupPolicyConfigPath
         warning_count = $warnings.Count
         duration_ms = [int]((Get-Date) - $started).TotalMilliseconds
     }
@@ -216,6 +218,7 @@ catch {
         status = "Failed"
         server = $ServerName
         database = $DatabaseName
+        backup_policy_config = $script:AppliedBackupPolicyConfigPath
         error_message = $_.Exception.Message
     }
     throw
