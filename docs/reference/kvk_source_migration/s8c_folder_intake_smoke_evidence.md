# S8C approved disposable SQL and folder intake evidence

## Current status — S10D SQL merged; S10E scope next, 2026-09-15

SQL #87 is merged and locally pulled at `80353a6280e523f30c27e724f71e7b47dadadd16`.
Bot main/origin main remains `bf3eccf964601e2975dd86eefe96f7b0153be3bb`; production/main remains
`aa1821adbde9ccda47797caa83b5d5a9958bfce9`. **No changes have been pulled to the bot machine.**
S10D authoring, offline checks and Changes security review are complete; SQL installation and
provider/Discord execution are not established. S10C remains source/static SQL evidence too.
See [S10D closeout and exact carry-forward manifests](s10d_closeout_and_s10e_handoff.md).

Next: **S10E Export Operator UX and Rollover, initial review/scope only**:
[task pack](../../task_packs/Codex%20Task%20Pack%20-%20KVK%20Source%20Migration%20S10E%20Export%20Operator%20UX%20and%20Rollover.md) and [starter](../../task_packs/Codex%20Chat%20Starter%20-%20KVK%20Source%20Migration%20S10E%20Export%20Operator%20UX%20and%20Rollover.md).
The eventual authorized S10E Bot implementation PR MUST include every pending Bot document in
that closeout, this pack/starter and all required archive identities. Verify filename AND
previous_filename, or exact merged/content and absent-at-base proof. No standalone docs PR,
repository mixing, manufactured implementation or renewed predecessor/grouping approval.
Both SQL documents were delivered in #87; their new closeout edits stay in SQL for the next
actual authorized SQL implementation PR. S10E scope must assess any genuine SQL delta separately.

Preserve S6-OPS01/PERF01/CAP01, both uncertain publications and every retained database/file.
S8A six scripts, S8B 50 cases/actual restore versus offline history, and S8C seven local checks
remain distinct; none is new live Discord or deployment evidence. Shutdown stops admission and
drains owned delivery; uncertainty retains claims. No lease-age/job-state release.
This documentation closeout authorizes no implementation, Git publication, SQL/provider/Discord
execution, real import/export, bot-machine action, deployment, activation, predecessor rerun or
new task. Earlier dated checkpoints are historical; the current closeout controls the next step.

> Historical handoff: **S9B repository-delivered; not deployed**. See [S9B closeout / S10A scope and exact documentation carry-forward](s9b_closeout_and_s10a_handoff.md).
> Retained evidence and historical approvals below remain intact; no predecessor rerun or live operation is authorized.

> Retained S8B/S8C reference, 2026-09-14: S9B repository delivery is now complete.
> See [S9A closeout / S9B scope and pending documentation delivery](s9a_closeout_and_s9b_handoff.md).
> Historical execution/operator evidence and limits below remain intact; this update does not
> request a rerun or claim live Discord acceptance, bot-machine deployment or activation.


**For the hands-on walkthrough, use the [step-by-step work instruction](s8c_operator_work_instruction.md).**
This file records technical evidence; you do not need to follow its execution records.

## Scope and identity

Executed on 2026-09-13 after explicit operator approval of `9SX2VF4\K98DEV`,
`K98_S8C_Disposable_20260913_intake`, the separate `_restore` database and
`C:/K98-S8C-Smoke/20260913`. Bot source HEAD was
`b6e45293348ece7b562e29c0451a8c3a7dbe2550`; SQL HEAD was
`3c1b5ceaa7a686bc594ca8637d0a1569030d66c4`. No deployed Bot code changed.
The only new executable is the local transport adapter plus its offline tests.

Real `SourceAdminService`, `SourceAdminReviewService`, their DALs, parsers, artifact store and
S8B matched-update/publication services were exercised with real disposable SQL transactions.
Actor 10, guild 20 and channel 30 are **synthetic test identities**. Recorded confirmations and
counterpart attestations are synthetic test inputs, not Chris Watts's interaction or live membership.
Synthetic seasons 900001 (initial adapter failure) and 900002 (successful journey) remain retained.

## SQL execution

- Both database names and evidence root were unused before creation; K98DEV was already running.
  No service start/restart occurred. SQL service Modify access was granted only to the new backup folder.
- Built a fresh empty prerequisite fixture from five authoritative legacy table snapshots, accepted
  S2A/S2B definitions and five current S8A/B table snapshots. The exact file-byte hashes are in
  `evidence/setup_started.json` and `sql_setup_result.json`. S8A/S8B installers and predecessor
  validation packs were **not** rerun; snapshot construction is not new predecessor migration evidence.
- Backed up the 35-table prerequisite fixture using COPY_ONLY/CHECKSUM, passed VERIFYONLY and
  actually restored it into the separate approved `_restore` database with unique physical files,
  without REPLACE. Backup `backups/s8c_prerequisites.bak` SHA256:
  `8625c5f73b1687439022634ac3e80c5e90b5d3e68cc1f56bb79476bfff7d895f`.
- S8C migration `20260913_002_kvk_source_admin_reviews.sql` applied and reran successfully.
  Result: 36 tables, zero disabled/untrusted checks, zero leftover fixture review rows.
  The standalone S8C SQL rollback fixture passed in its own session.
- The 13-check static contract validator passed. Five S8C opt-in SQL tests passed; all 50 S8B
  integration tests were deselected. S8B opt-in was explicitly disabled.
- Direct execution did not exercise deployment-runner history. The restore database remains
  prerequisite-only, deliberately separate from the primary's S8C migration and intake data.

## Observed intake results

| Exercised case | Result |
|---|---|
| Missing fixed source | Rejected: season source setup required before admission |
| Baseline without kingdom/camp/weights | Metadata acceptance blocked; receipt cancelled and retained |
| Fixed source and configuration | Explicit source review; synthetic kvk_list output tables and real retained snapshot hook; accepted B0 and froze setup |
| UTC / retained observations | Explicit UTC starts produced scans 1–18; intake alone did not publish |
| Missing aggregate | Durable UpdateID `301f2637-65ec-47c9-a850-7fc7f1d74119` waited, then selected the complete pair with that same ID |
| Endpoint sequence | Actual publications 11−10, 12−10, 13−10, then approved endpoint change and 14−10; no additional correction command |
| Later assignment | Already retained scans 17/18 subsequently configured as Fight Two and calculated; scan 15 remains unused |
| No-fight | Equal 16/16 endpoints produced not-applicable aggregate state without aggregate input |
| Overall | Live overall rejected; explicitly finalized independently supplied overall accepted |
| Two files, both orders | Player-first and aggregate-first two-attachment equivalents accepted as retained semantic duplicates; no extra scan |
| Byte-different re-export | Changed XLSX ZIP bytes retained, same semantic observation revision and scan 14 returned |
| Resume / permissions / CAS | Separate script processes and fresh service instances read durable state; wrong actor, channel and version rejected; cancellation retained originals |
| Weights after use | No-attestation review remained pending; explicitly attested compatible replacements changed player DKP 492 → 892 for 14−10 |
| Kingdom and camp authority | Kingdom DKP stayed 25.3M (overall 99M); independently supplied camp DKP stayed 4.779B / 1.20M, not kingdom sums or player calculations |
| Camp mapping after use | Swapping camp IDs did not reuse incompatible aggregate even with attestation; explicitly corrected aggregate plus exact player-counterpart attestation replaced Fight One with camps 2/1 and DKP 892 |
| Atomic selection / intent | Every selected result had its complete intent vector; first 11 vector lengths were 1,1,1,1,2,3,4,4,4,4,4 |
| External effects | SourceRouting remained disabled; zero SourceDelivery rows; legacy KVK_Scan remained empty; no provider adapter or bot startup |

After the camp-change test, Fight One is complete under the corrected map (commit sequence 12).
The other three periods deliberately retain prior complete selections with pending map requests.
This is retained negative-path evidence, not a fully activated season. No pending request is to be
completed or deleted by a cleanup script. `final_readback.json` is the pre-map-change checkpoint;
phase 5's transcript records the later map correction separately.

## Failures retained and coverage limits

No production implementation defect was found in the exercised paths. Retained harness failures
were an overlong synthetic message ID, an inspection query using the wrong timestamp column,
attempted live overall metadata (correctly rejected by the domain), and a camp-total expectation
that incorrectly assumed kingdom values. Each failure and successful correction/resumption is retained.
The initial missing-source rejection from the overlong-ID attempt is not counted as source-gate proof;
the subsequent successful run records the actual source-gate rejection.

The two-file order checks used replays of accepted inputs; they do not independently prove every
fresh-arrival permutation. Existing mocked Discord interaction tests remain separate. No live Discord
uploads, role lookup, modal rendering, real Google kvk_list import, full bot restart or concurrent
two-connection race test occurred. Daily SCANORDER and legacy admission lifetime were not newly
exercised end-to-end by this folder harness. No real workbook import/export or provider delivery ran.
Operator hands-on acceptance is recorded separately below; it does not extend the automated
coverage or constitute live Discord acceptance.

S8B operator acceptance and 50-case SQL/actual-restore evidence remain separate from its final offline
fixes and offline-only SQL runner-history support. S8A six-script/VERIFYONLY evidence is unchanged.
Preserve S6-OPS01/PERF01/CAP01, both uncertain publications, all predecessor databases/files, and
all new smoke artifacts. No bot-machine pull, activation, restart, deployment, Git publication or
S9/S10 implementation occurred.

## Retained artifacts and console

Root: `C:/K98-S8C-Smoke/20260913`. Evidence includes `setup.py`, `setup_started.json`,
`sql_setup_transcript.log`, `sql_setup_result.json`, `s8c_sql_tests.log`,
`s8c_static_sql_contracts.log`, phase scripts/logs, failed-attempt scripts/logs,
`console-*.jsonl` with receipt/review/UpdateID data, `final_readback.json` and the file-byte manifest.
Original synthetic workbooks remain in `inbox/`; content-addressed retained originals in `artifacts/`.
Phase scripts are retained execution records, not scripts to rerun blindly against existing seasons.

For exact commands, expected results and the checklist to return to Codex, use the
[operator work instruction](s8c_operator_work_instruction.md). It covers submitting the existing
synthetic files, reviewing UTC metadata, explicit acceptance, reading the saved pair and cancelling
a new pending receipt. It does not ask the operator to rerun setup or write SQL.

## Local validation and delivery

Adapter and review focused tests: 43 passed. Architecture, deferred-items, security-routing,
test selector, imports and command registration passed (36 top-level / 101 grouped); Ruff passed.
Full offline suite: **4,223 passed / 62 skipped**; the separate log-isolation run repeated that
result and proved production operational logs unchanged. Bot Changes review
`1f04c523-182e-44b5-a164-7399484ef086` completed with **zero findings**, Deep off,
working-tree baseline `b6e45293348ece7b562e29c0451a8c3a7dbe2550`; scan snapshot digest
`e22ef0b2a6b4d363db5280b6706bb5d2770921d671524bc42f4041ee64f5d84c`.
Daybreak status was granted (Daybreak Blue). Measured scan usage was 1,135,678 tokens, including
1,083,392 cached input tokens; this is workbench-reported usage, not a billing calculation.
SQL independently received a documentation-only skip: only `docs/SQL_DELIVERY_LOG.md` and
`migrations/README.md` changed; no SQL runtime, configuration or permission definition changed.
This final documentation-only validation addendum was written after the sealed scan; the reviewed
Python files are unchanged. Canonical scan artifacts are retained under `evidence/security/`.
The exact pending Bot union is maintained in [the closeout](s8c_closeout_and_s9a_handoff.md),
including this evidence file, console adapter and tests. SQL delivery-log and migration-index edits
remain separate SQL PR carry-forward. Everything remains unstaged and uncommitted.


## Operator walkthrough accepted — 2026-09-14

Chris reported **PASS on all seven work-instruction checks**: console startup, submission of both
files, player duplicate scan 14 confirmation, aggregate duplicate confirmation, inspection of the
selected 10–14 pair, cancellation of a new pending upload, and the pair remaining selected afterwards.
This closes the local operator walkthrough for the exercised review/accept/cancel/readback steps.

Retained transcript, read back without SQL execution:
`C:/K98-S8C-Smoke/20260913/evidence/console-44866605-bf6b-4c20-980c-0214ff75f383.jsonl`.
SHA256: `0d5177f2adc89573b9d2229739853c8f8eb4772116df796b2aa38dc716fe72c4`.
The transcript is retained separately from the earlier automated-run hash manifests.

| Observed item | Retained identity/result |
|---|---|
| Player receipt | `eb86cd6d-d348-42fc-8847-a9b54264a28f`; duplicate, scan 14 |
| Aggregate receipt | `bb1004b6-d060-48da-a248-9b7c933a04bf`; duplicate aggregate revision `04c5b3c9-1fbf-4769-8e21-94920805498b` |
| Cancelled pending receipt | `d602bf78-15e8-444d-924c-b6c193100be4`; cancelled true, version 2 |
| Existing complete UpdateID | `1c450338-05c2-4248-9d25-5885cc3c008d`; selected, version 3 before and after cancellation |

One confirmation attempt was rejected because the phrase did not match the exact kind/UUID/version;
the subsequent explicit aggregate confirmation succeeded. The rejected attempt remains in the
transcript as evidence of the confirmation guard, with no unresolved error reported by the operator.

Chris's acceptance is evidenced by his returned checklist. The console continues to label its actor
and confirmations synthetic; that label is not rewritten into live Discord identity or new counterpart
attestation. This walkthrough replayed accepted files and inspected an existing pair. It did not
create a fresh baseline or pair, exercise live Discord, deploy, activate, or touch the bot machine.

This acceptance-record update changes documentation only. No SQL or tests were rerun, and no new
security scan was required for these evidence/status edits. Existing code-review/test evidence is
unchanged. All pending files remain uncommitted and unstaged.
