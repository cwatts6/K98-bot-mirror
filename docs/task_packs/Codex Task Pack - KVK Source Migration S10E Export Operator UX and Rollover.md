# S10E — Export Operator UX and Rollover

## Approved implementation and evidence boundaries — 2026-09-15

The operator subsequently approved the concrete SQL ownership amendment and its two additional
Bot caller/test paths, then instructed continuation. The approved local scope is **24 Bot
implementation paths plus the exact mandatory documentation union, and eight separately scoped
SQL paths**. The historical approval-required checkpoint below is superseded by this approval;
it is retained to explain why the typed third owner was necessary. No renewed approval is
required for these local edits/tests. Git publication, live SQL/provider/Discord execution,
bot-machine changes, deployment and S11 activation remain outside this authorization.

### Authored behavior

- Existing grouped source command v1.01 owns six admin export/rollover actions; no top-level
  command. Ordinary source-aware export_all delegates through the same operator admission.
- Export pins accepted intent/input hash and exact pool/registration/epoch. Confirmed duplicate
  work is a zero-provider, zero-materialization no-op. Pre-attempt retry preserves JobID/ticket;
  attempted uncertainty requires reconciliation. Explicit confirmed damage gets a fresh RepairID
  and physical attempt key while preserving original accepted content and byte-exact receipts.
- Status reads SQL only. Five-minute previews pin authority and exact state; restart expires an
  unconfirmed token. Durable confirmed OperationID/RepairID replay rechecks actor/guild/channel
  and returns the retained outcome without admitting duplicate work.
- Rollover closes admission and audits only never-started pending cancellation. An independently
  verified drain precedes fair account/file operation ownership. Durable private/clear/setup
  phases surround provider calls, with no SQL transaction over requests or pacing waits.
- Complete private empty-file and setup-marker readback precedes separate append-only retirement
  and clear events, exact owner/fence/version CAS, and a single monotonic epoch increment.
  Uncertainty retains claims; terminal reconciliation does not repeat provider mutations.
- P/Q/R capacity uses the actual complete partition plan: `1+P+P+max(P,Q)+P+R`, with 9,000,000-cell
  parts, eight registrations and sixteen slots. Quarantined files remain registered as Q;
  other retained assigned representations remain R until explicit audited retirement. They
  cannot become free through lease age, job state, receipt guesses or generation relabeling.
- A proven terminated reconciled writer can release its exact slot owner while retaining its
  quarantine, assignment and disposition. Partial rollover evidence and quarantined/retired
  slots remain blocked; there is no automatic quarantine reuse or blind retry.

### Static versus transaction/provider matrix

| Boundary | Authored/offline evidence | Still requires separately authorized execution |
| --- | --- | --- |
| SQL shape and membership | Separate migration, operation/resource snapshots, third exclusive ExportResource owner; 428 offline contract assertions; T-SQL parse of five files and constant embedded payload | Exact installation/rerun/drift/rollback behavior on a newly approved disposable target |
| Ownership/CAS/fairness | Exact scoped DAL predicates, 178 constant SQL-call parameter-arity checks; deterministic stale-owner, uncertainty, ticket and caller tests | Real concurrent connections, lock behavior and commit acknowledgment outcomes |
| SQL transaction scenarios | Independent S10E opt-in cases for two workers, stale owner/fence/version/pool version, retained uncertain claims, daily/preparation contention and closing-confirmation replay | None of these S10E SQL cases has executed; their distinct seeded targets/identities and backup/actual restore need approval |
| Provider lifecycle | Real adapter methods exercised using stateful fake clients for private ACL, removing old grids/names/metadata, full empty readback, setup marker and rejected contamination; SDK calls retain shared pacing/ownership hooks | Actual account ACLs, queued/delayed requests, complete remote readback and old-writer termination |
| Permissions/UX | Fresh member/config authority, no uploader privilege inheritance, expiring and durable confirmations, no-op/rebuild/reconcile tests; actual command serialization and static inventory | No Discord login, command publication or interaction execution |
| Preservation | Original recovery/closeout evidence retained; exact old/new content, source/archive and absent-at-base checks control carry-forward | Eventual Bot PR must carry every mandatory identity; SQL documentation belongs only with its separately scoped implementation |

### Composition, rollback and handoff

Production factories remain closed. Explicit future composition must supply installed/attested
SQL, exact dedicated registered pool/file/slot state, all compatible account writers, durable
spool/budget ownership, and independent termination/readback evidence. This patch does not
bootstrap registration by guessing historical provider mappings, start workers, attest a live
installation or expose an activation flag. Capacity/setup errors preserve retained files.

Rollback is stop admission, drain owned delivery, reconcile uncertainty and forward fix.
Do not remove owner schema while claims exist, reset branches/indexes, detach old threads,
delete retained data or restore mutable old provider labels. S6 open gates/both uncertain
publications, S8A six scripts, S8B 50 cases/actual restore versus offline history, and S8C seven
local checks remain distinct; none becomes new deployment/provider evidence.

Security routing is **Changes, Deep off**, separately for the exact Bot and SQL patches. SQL
scan `7bbcbc69-31c1-40cb-a7a3-bf8f646a234e` completed with no findings on digest
`codex-security-snapshot/v1:sha256:02bd91e5eab19c17fabf886b83b9a929ec56c48f7771a49f8046a6698f2938ae`.
The Bot gate requires its own sealed report for the final target; SQL review is not Bot approval.
The final handoff carries actual suite/validator results and exact evidence paths. Earlier
scope/checkpoint evidence below is historical and must not be substituted for those final gates.

### Local validation checkpoint before Bot security review

`scripts/analyse_pytest_log_noise.py --pytest-args -q tests`: **4,578 passed, 71 skipped**
in 202.80 seconds; all four operational log files unchanged. The preceding run exposed one
related missing-roles test-double failure; actor construction now treats missing roles as an
empty set, and the final full run passes. SQL execution gates stayed disabled.

Architecture (21 Python paths), deferred-item validation, security-routing (zero errors/warnings),
test selection, import smoke and command inventory passed. Constant SQL-call arity checked 178
calls. SQL contract checker passed 428 assertions; syntax parsing passed five SQL files plus
the embedded DDL payload. Four retained offline negative variants (wrong epoch, nullable plan
hash, inherited receipt drift and missing membership snapshot) were rejected. These are text
and fake-client checks, never transaction/provider installation proof.

Exact preservation evidence is retained at
`C:/Users/cwatt/AppData/Local/Temp/k98-s10e-authoring/implementation-manifest.json` and
`implementation-pending-files.zip`, alongside `static-sql-parse.json` and
`negative-contracts.json`. This supplements, never replaces, the original recovery and S10D
closeout archives/manifests. Each current filename/action/hash and baseline blob or exact
absence is recorded, including both S10C source/archive sides, S10D archives and S10E files.
The 75 Bot paths and eight SQL paths match the exact approved unions. All comparison anchors
and both empty indexes remain unchanged. The later eventual PR must verify filename AND
previous_filename or the retained exact content/merged/absent-at-base proofs; counts alone
are insufficient.

## Implementation approval and SQL ownership checkpoint — 2026-09-15

The operator approved the reviewed local S10E implementation and offline validation plan:
**“approved please proceed”**. This approves the original twenty-path Bot proposal, the two
caller/test additions `kvk/services/kvk_admin_service.py` and `tests/test_kvk_admin_service.py`,
and every mandatory pending Bot documentation identity. Five-minute previews expire on restart;
confirmed operations require durable evidence. The earlier initial-scope-only text below is
historical. Publication, SQL/provider/Discord execution, imports/exports, deployment, activation,
predecessor reruns and bot-machine actions remain unauthorized. S7/predecessor acceptance and
documentation grouping remain settled.

The approved plan explicitly requires stopping to scope a genuine schema gap. Detailed DAL
design reached that checkpoint before runtime edits: **rollover has no typed shared-resource
owner in the current schema**. This corrects the initial scope's provisional no-migration
assessment; S10D's accepted static contract is not being reopened or rewritten.

### Concrete failure boundary

After rollover confirmation, old admission must close while an existing export drains. After
drain, rollover needs durable account and file ownership across ACL changes, clear, readback,
request waits and process loss. A pool owner alone does not participate in the shared
ExportResource membership/ownership contract used by legacy and daily preparation callers.

Authoritative SQL at `80353a6280e523f30c27e724f71e7b47dadadd16` shows:

| Existing object/constraint | Exact limitation |
|---|---|
| `dbo.ExportResource.CK_ExportResource_Ownership` | An owned resource must reference either ActiveJobID or ActivePreparationID, exclusively. There is no pool-operation membership/owner branch. |
| `dbo.ExportJob.CK_ExportJob_Consumer` / `CK_ExportJob_Scope` | Only new_source/all_kvk/scan_data; new_source requires an accepted IntentID and export input/season/epoch. A rollover is not an export or a repair of an accepted vector. |
| `dbo.ExportPreparation.CK_ExportPreparation_Consumer` / `CK_ExportPreparation_Scope` | Only all_kvk/scan_data/config with their existing capture meanings. Reusing config for rollover would misclassify work and bypass its scope. |
| `KVK.SourceOutputPool` | Current owner/state/registration exists, but no immutable confirmed operation plan or account queue ticket. RegistrationJson/RegistrationHash describe registration, not mutable workflow progress. |
| `KVK.SourceOutputDisposition.CK_SourceOutputDisposition_Action` | Only retire/quarantine/clear/assign. A normal closing request is not evidence that one of those file dispositions occurred. OperationID groups events but does not reference a durable operation record. |
| `KVK.SourceAdminReview.CK_SourceAdminReview_Kind` | No export/rollover kind; extending preview persistence alone would still not supply shared-resource ownership. |

The actual callers confirm the gap: `ExportCoordinationDAL.claim_next` and
`LegacySnapshotDAL.claim` inspect and CAS job/preparation ownership on the shared account and
destination rows. Their ticket selection covers ExportJob and ExportPreparation only.
Do not implement a third ownership mechanism in BlockedReason text, a registration JSON rewrite,
an invented export/config job, or a fabricated quarantine event. An application lock held only
within a short SQL transaction cannot protect provider work after that transaction ends; holding
the transaction over provider work is prohibited. Restart-expiring previews do not solve the
durability of an already confirmed rollover.

### Separate SQL authoring scope — subsequently approved

Add a typed pool operation and resource membership, then extend shared ownership with a mutually
exclusive ActiveOutputOperationID branch. Preserve all existing job/preparation meanings.

`KVK.SourceOutputOperation` will retain OperationID, PoolID/account scope, old/new fixed season
choices, old/target epoch, immutable confirmed plan/hash (including exact files, receipt/job
identities and expected versions), actor/guild/channel/reason, server-UTC confirmation, a shared
account EnqueueSequence, owner/fence/version and explicit closing/ready/running/blocked/uncertain/
completed state. Progress must record the exact phase/current file and request evidence before
provider mutation; completed per-file outcomes remain append-only dispositions. Bound JSON at
65536 UTF-16 bytes and fail before mutation on exhaustion. Reject changed replay payloads.

`KVK.SourceOutputOperationResource` will retain immutable operation membership for the account
and exact registered file resources. Scoped keys must bind operation/pool/account and file
membership; ResourceKind and live owner agreement still require transaction checks. Add the
matching composite active-membership FK from ExportResource. Use a filtered unique active-pool
key to prevent overlapping nonterminal operations. Operation facts are immutable; state/progress
updates use expected owner/fence/version with exactly one returned row. The operation owns no
provider resources while an old writer drains; readiness/claim requires termination and remote
outcome proof, then fair account admission. No automatic takeover or lease-age release.

The migration must add and validate exact inherited S10A/C/D shape, leave all existing application
rows/receipts/claims intact, and reject partial or drifted state without sealing it. New nullable
ownership defaults preserve existing rows. All active ownership alternatives are mutually
exclusive; existing readers/writers must be upgraded before this new owner can be admitted.
No new ProcConfig row, UDT, export consumer, database grant or activation setting is proposed.

Exact proposed SQL paths (SQL repository only):

| Action | Directory | Filename |
|---|---|---|
| Create | migrations | `20260915_002_kvk_output_operation_ownership.sql` |
| Create | sql_schema | `KVK.SourceOutputOperation.Table.sql` |
| Create | sql_schema | `KVK.SourceOutputOperationResource.Table.sql` |
| Modify | sql_schema | `dbo.ExportResource.Table.sql` |
| Create | validation/kvk_source | `s10e_output_operation_ownership.sql` |
| Create | deploy | `Test-OutputOperationOwnershipContracts.ps1` |
| Modify | docs | `SQL_DELIVERY_LOG.md` |
| Modify | migrations | `README.md` |

The date/ordinal is a proposal: 20260915_001 is the only currently present migration for this
date. Recheck the actual creation date/free ordinal immediately before authoring. Never rename
the merged S10D migration. Both already pending SQL closeout documents belong in this genuine
SQL implementation PR; no standalone documentation PR or Bot/SQL history mixing.

Additional Bot scope required by the ownership change, beyond the already approved 22 paths:

| Action | Exact Bot path | Reason |
|---|---|---|
| Modify | `services/legacy_export_snapshot_dal.py` | Include operation ownership in resource admission/CAS and operation tickets in the existing account fairness ordering. |
| Modify | `tests/test_legacy_export_snapshot.py` | Assert mutual exclusion, preserved legacy/daily tickets and uncertainty retention with an active pool operation. |

Other integration stays in the approved pool/operator/coordinator DAL/service/test paths. The
proposed Bot union is therefore 24 implementation paths plus the exact mandatory documentation
union (75 physical identities after the three overlapping documentation paths are deduplicated).
Exact path/action/content proof remains controlling. No additional file is authorized merely by
this proposal. Existing recovered evidence and both closeout manifests remain intact.

### Required validation and execution boundaries

Offline SQL authoring checks must cover inherited drift/partial installation, exact snapshots,
membership scope, each exclusive owner alternative, unchanged historical receipts, active-pool
uniqueness, JSON/counter bounds and intentionally corrupted schema variants. Author distinct
S10E opt-in transaction cases for two workers, job/preparation/rollover contention, lost closing
acknowledgment, preserved ticket age, stale owner/fence/version and uncertainty retention.
Mocks/static checks do not prove SQL concurrency, installation or provider behavior.

Later execution requires its own exact disposable target/operation approval, reviewed hashes,
backup and actual restore, dependency/lock plan and preservation checks. S10C/S10D installation
remains unproven; missing prerequisites stop execution rather than authorizing predecessor reruns.
Never use retained S6/S8/S10A databases/files as new targets. Rollback remains stop admission,
drain/reconcile and forward fix with all history/uncertain claims retained.

Security routing remains separate **Changes, Deep off** for the actual SQL amendment and the
actual Bot implementation. Resolve each exact base/head or sealed patch when ready. This current
checkpoint changes only the S10E pack/starter, so it has a documentation-only scan skip; it does
not satisfy either later runtime review gate. No runtime code or SQL source was authored before
this checkpoint, and no runtime tests or provider/SQL/Discord operations were run.

Checkpoint validation: architecture passed (zero Python paths); deferred-items passed (52
Markdown files); security-routing passed (zero errors/warnings); test selector completed.
Its smoke-import/registration suggestions and pytest are skipped for this documentation-only
checkpoint: no runtime, options, dependencies or configuration values changed. The earlier
closeout hash manifest stays immutable; a new checkpoint snapshot records the intentionally
updated S10E pack/starter while verifying every other pending path/action/hash unchanged.

## Status and authorization — 2026-09-15

**Prepared for initial review/scope only.** S10D SQL #87 is reviewed, merged and locally pulled
at `80353a6280e523f30c27e724f71e7b47dadadd16`; final PR head `0eb5494976627bcb6efaea40e11adcb0cf231344`.
Bot main/origin main remains `bf3eccf964601e2975dd86eefe96f7b0153be3bb`; production/main remains
`aa1821adbde9ccda47797caa83b5d5a9958bfce9`. **No changes have been pulled to the bot machine.**
These are comparison anchors, not reset instructions. Preserve all pending work and the recovery archive.
S10C/S10D SQL merge/static evidence is not database installation, execution or activation proof.

Read the [S10D closeout and exact documentation manifests](../reference/kvk_source_migration/s10d_closeout_and_s10e_handoff.md) and
[starter](Codex%20Chat%20Starter%20-%20KVK%20Source%20Migration%20S10E%20Export%20Operator%20UX%20and%20Rollover.md). Return scope, affected layers, exact implementation manifest,
risks, tests, migration/rollback implications and implementation plan for approval. Do not implement,
publish Git changes, execute SQL/provider/Discord, run imports/exports, pull/restart the bot machine,
deploy, activate, rerun predecessors or start a new task automatically. No S11 implementation.
No renewed approval of settled S7 contracts, accepted predecessors or documentation grouping.

## Required references and skills

Read current AGENTS, README-DEV, reference index and every core engineering/execution/testing/
skills/deferred reference it requires. Apply k98-architecture-scope, k98-sql-validation,
k98-test-selection and k98-security-review-routing; use the command-feature skill when scoping
permissions, confirmations or registration. Read approved S7 integration contracts/manifests,
phase-2 architecture and EndScanID amendment, retained S6 release/rollback evidence, S8A/B/C
closeouts and S10A/B/C/D handoffs. Inspect authoritative current SQL in C:/K98-bot-SQL-Server,
including both delivery documents and the merged S10D migration/snapshots, rather than infer schema.

## Exact S7 starting manifest; reconcile before implementation approval

| Repository | Action | Exact path |
|---|---|---|
| Bot | Create | `kvk/dal/source_output_pool_dal.py` |
| Bot | Create | `kvk/services/source_output_pool_service.py` |
| Bot | Create | `kvk/services/source_export_operator_service.py` |
| Bot | Create | `ui/views/kvk_source_export_view.py` |
| Bot | Create | `tests/test_kvk_export_operator.py` |
| Bot | Create | `tests/test_kvk_output_rollover.py` |
| Bot | Modify | `commands/stats_cmds.py` |
| Bot | Modify | `kvk/services/new_source_admin_service.py` |
| Bot | Modify | `kvk/services/new_source_export_service.py` |
| Bot | Modify | `kvk/services/new_source_delivery_service.py` |
| Bot | Modify | `kvk/dal/new_source_delivery_dal.py` |
| Bot | Modify | `services/export_coordination_service.py` |
| Bot | Modify | `services/export_coordination_dal.py` |
| Bot | Modify | `docs/reference/canonical_command_reference.md` |
| Bot | Modify | `docs/reference/ENV_REFERENCE.md` |
| Bot | Modify | `docs/reference/runbook_diagnostics.md` |
| Bot | Modify | `tests/test_kvk_source_admin.py` |
| Bot | Modify | `tests/test_kvk_source_delivery.py` |
| Bot | Modify | `tests/test_kvk_export_sql_integration.py` |
| Bot | Modify | `tests/test_kvk_export_coordination.py` |

The twenty-path S7 table is the starting implementation scope, not permission to create everything
without inspection. Reconcile current callers, tests and helpers; add only justified paths to the
approval manifest. Mandatory documentation below is a union with this table, not a replacement.
S10D already delivered four pool/file/slot/disposition tables and two attempt reference keys.
Do not rerun or rewrite its migration. If a genuine schema gap is found, report it and scope a
separate SQL implementation target with its own migration date/free ordinal, docs and security review.
No SQL runtime delta is presumed or manufactured to publish pending SQL documentation.

## Architecture and SQL contract

Commands/views enforce guild/channel/admin role, defer safely and recheck actor and authority at
confirmation; services receive typed authorization context, never Discord objects. DAL owns SQL,
short transactions, expected-row predicates and row counts. Reuse current coordination/provider
adapters, source/delivery services, registration and helpers; keep legacy wrappers thin. Do not add
business rules/SQL to views or command modules, unrelated refactors or new top-level commands.

Validate SourceOutputFile's unique BIN2 physical FileID and fixed index/slot role; exact destination
resource mapping; SourceOutputPool account/index/registration/season and current owner/fence/version;
SourceOutputSlot current assignment/event/part/epoch/version pointers; SourceOutputDisposition
scoped file/pool/account/old-new season/job/attempt/part/membership and optional historical delivery.
Existing ExportAttempt/Part composite keys bind exact identities. Historical events must not depend
on mutable current pool season/epoch. ResourceKind and live owner agreement require transaction checks.

SQL static constraints establish shape, uniqueness and complete FK scope. S10E must supply atomic
owner/fence/version CAS with exactly one affected row, monotonic epochs/sequences/versions and
append-only assignment/disposition APIs. Reject stale owners/epochs, overflow and replay conflicts.
Do not UPDATE/DELETE event history, relabel mutable generations or rewrite historical receipt bytes.
Reconcile padded/aliased provider identities to the canonical registered file before using a URL or
provider ID; do not guess old receipt mappings. Absent or ambiguous history stays blocked/quarantined.

Use established account admission -> sorted destination/pool resources -> short job/CAS ordering;
preserve independent season/publication lock order without inversion. Do not hold SQL transactions
over waits, pacing, SDK requests, grants, clears, pointer publication or readback. SQL fences cannot
cancel requests already submitted to a provider. No lease-age/job-state release or automatic takeover.

## Grouped export operator workflow

Extend existing /kvk_admin source actions for export, export_status, export_reconcile,
export_rebuild, rollover_preview and rollover_confirm; review final names/signature and actual
registration counts. Preserve existing choose_source/match_update and source-aware ordinary exports.
No top-level allowlist expansion. Status is read-only and reveals no private rows, storage paths or keys.

Export pins a complete accepted vector and registered destination without parsing, importing,
recalculating, moving endpoints or publishing Discord. Confirmed same generation is a zero-provider
no-op. Safe pre-mutation retry retains identity. Uncertain attempts require exact readback/reconcile,
never blind retry. Damaged confirmed output needs explicit preview/confirmation/reason, expected
source/vector/generation/epoch and fresh RepairID while retaining original receipt and repair audit.
Uncertain rebuild is denied. Distinguish queued/running/waiting counterpart/confirmed/no-op/blocked
and expose a current link only when confirmed. Stale buttons, wrong actor/guild, removed role,
expired token or restart without durable authority cannot enqueue. Persist confirmations or expire safely.

## Pool capacity and explicit rollover

Preserve `1 + P + P + max(P,Q) + P + R`: stable index, active, staging, quarantine, reserve and
other protected references. Derive P from the actual complete partition plan, Q from quarantine,
R from retained protected references. Preserve 9,000,000 cells per part, eight registrations and
sixteen slots per registration. Closed/blocked registrations still consume their SQL ordinal.
Check all file/part/receipt capacity before claim/provider mutation; exhaustion returns setup required.
Normalized multipart receipts never truncate/reformat the historical bounded SourceDelivery receipt.

Preview pins exact old/new season/choice/epoch, versions, authority, jobs, receipts and file IDs.
Confirm closes old admission, drains/reconciles owned delivery, and audits cancellation only of
never-started pending exports. Retain publications and inputs. Require terminated-old-writer proof
before epoch advancement or reuse; detaching a late thread is not that proof. Uncertain account,
ACL, clear or pointer outcomes retain blocked/quarantined ownership and prevent conflicting advance.
Reuse needs explicit private ACL verification, complete removal of prior tabs/data/formatting/named
ranges, replacement with expected empty manifest and readback before assignment. Append audited
retirement/clear/assignment events. Old URLs mean retired/reused, not permanent archives. Never
silently edit/delete prior Discord messages or replace an uncertain stable index.

## Preserved producer and domain invariants

Keep immutable running A, eligible pending B/C coalescing with retained history/ticket age,
account fairness, daily SCANORDER/history without coalescing, scoped receipts, registration-aware
intent lifecycle and one-period compaction. Preserve S10C complete immutable output/config/header/
provenance capture, nested owner tokens, UPDATE_ALL2 transaction ownership, durable spool digest/
length/owner, pending/unavailable evidence and server-UTC SDK reservation/completion/cooldown.
No ScanID-only completion inference, mutable per-tab reread or advanced-generation relabeling.
Shutdown stops new admission and drains owned delivery; uncertain and late-thread claims stay held.

Preserve fixed source per season, independent consumers, supplied overall/B0, authoritative aggregates/
DKP, UTC/window rules, exact 11-10 / 12-10 / final 13-10 / authorized 14-10 endpoints, sealed inputs/CAS,
matched UpdateID and explicit counterpart attestation. No mixed sources, summed-fight overall,
silent legacy fallback or SourceRouting.Enabled-only activation.

## Tests and review gates to propose

Map S7-T12/T13 to operator zero-API no-op, no-parser/no-calculation export, permission/restart/stale
confirmation boundaries, safe retry, denied uncertain repair, fresh RepairID and preserved receipt.
Map S7-T14/T15 to scoped FK/CAS, closing/drain/reconcile, late old writer, epoch advancement,
private/clear/readback uncertainty, append-only history, retired links, P=2 and larger partitions,
quarantine exhaustion, registration/slot and cell/receipt bounds before mutation. Test actual caller
wiring, two workers and lost acknowledgements with deterministic offline doubles; separate those from
real two-connection SQL and provider proofs. Keep inherited daily/producer/consumer regressions targeted.

Select focused tests from the exact manifest, command registration/permission/restart checks,
architecture/deferred/security-routing validators, exact-path test selection, lint and smoke imports.
Use the required isolated full-suite/log-noise workflow for broad integration; never overwrite
operational logs with expected test failures. Record actual outcomes, skips and evidence limits.
After implementation approval route security to **Changes, Deep off**, exact Bot base/head or immutable
patch. Any separately approved SQL delta needs a separate SQL Changes target. No inferred Codebase or
Deep scan. Documentation-only closeout has a precise skip; runtime permission/persistence changes do not.

Database/provider/Discord execution still needs separate exact target/operation authorization,
backup/actual restore and installation-order plan. S10D's 90 authored cases/seven fixture modes are
unexecuted; 463 static assertions/101 parse inputs/11 corrupted variants are not installation proof.
No retained S6/S8 database/file may become a new test target. Forward-fix schema/history; disable
new admission safely without releasing owned/uncertain resources. No destructive rollback.

## Mandatory documentation in the eventual implementation PR

**The S10E Bot implementation PR MUST include EVERY exact pending Bot path in the S10D closeout**,
including all recovered predecessor updates, the S10C and S10D archive destinations, S10C tracked
source deletions, this S10E pack/starter and both closeouts. Reconcile fresh staged/unstaged/untracked/
deleted files and extend the manifest for additional task-produced documentation. Verify GitHub
filename AND previous_filename and exact content at PR head/merge; counts are insufficient. S10D
active source paths were never committed at the recorded Bot/production bases: preserve explicit
absence/content proof rather than invent a rename/deletion entry. See the closeout's archive table.

No standalone docs PR, mixed repositories or invented Bot/SQL runtime work. Two newly pending SQL
closeout documents stay in SQL for its next genuine authorized SQL implementation PR; include them
there if S10E scope finds and approves a real SQL delta, otherwise carry them forward explicitly.
Documentation grouping and predecessor acceptance are settled and must not be re-requested.

Retain S6-OPS01/PERF01/CAP01 and uncertain publications e19c89ac-7977-5f28-ae4c-031807cd1728 and
54a2480a-26fb-5bad-a3f5-9321525a731c, all retained databases/files and evidence. S8A six-script/
VERIFYONLY, S8B 50-case/actual restore plus distinct offline history, and S8C seven local checks
remain distinct. No new live Discord, deployment or activation evidence is claimed.


## PR review corrections — 2026-09-15

Bot PR #280 review adds callback-level tests, confirmed-no-op messaging and closing-ticket admission barriers for jobs and preparation preflight. SQL PR #88 independently strengthens the authored CAS fixture with correct-owner stale-version/fence negatives and a successful current-version control. No SQL execution is claimed.

Ordinary superseded generations are eligible for audited in-season retirement only when their immutable attempt manifest explicitly records non-final retention (`retain=false`), their exact confirmed multipart receipt matches, and all assigned slots are active and unowned. Missing/older retention metadata, final representations, live references, uncertain/quarantined/retired slots remain protected. A trusted composed verifier must attest the exact snapshot, old-writer termination, current new pointer and absence of live references while the current job retains its full resource claim. There is no age/job-state release.

After new-pointer readback and before job confirmation releases resources, append retirement intent under owner/fence/version CAS, privately clear/read back each exact slot, then append its clear disposition before freeing it at the same epoch. Preserve original attempt/part receipts, assignments and accepted inputs byte-for-byte. Failure retains owned delivery and incomplete retired slots for reconciliation; no blind cleanup resume. Completed clear removes only K98 provider labels, preserving unrelated application properties, and assignments use the cleared blank representation. Capacity continues to reserve final/referenced and incomplete retired representations.

The delivery adapter now accepts an explicitly injected `retirement_verifier`; composition remains disabled until independent evidence producers and SQL/provider behavior are separately validated. Offline tests cover protected candidates, proof omissions, scoped CAS, provider/commit failures, repeated provisional two-part retirement, cleared-file rebinding and view callbacks. This does not authorize or prove installation, activation, provider execution, production promotion or bot-machine changes.
