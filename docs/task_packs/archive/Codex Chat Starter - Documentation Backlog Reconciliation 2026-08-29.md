# Codex Chat Starter — Documentation Backlog Reconciliation

Work in the current local `C:\discord_file_downloader` repository/worktree.

The operator has already archived these completed records:

- KingdomScanData4 Phase 5.2 task pack and chat starter;
- GovernorOS Phase 8.1 task pack and chat starter;
- MGE Process Polish Phase 2 initiation statement.

Do not recreate, restore, duplicate or repeat those archive moves from an older mirror snapshot.

Read and execute:

1. `Codex Task Pack - Documentation Backlog Reconciliation 2026-08-29.md`
2. current `docs/task_packs/README.md`
3. current `docs/task_packs/archive/README.md`
4. current `docs/task_packs/Player Self-Service Command Centre v2 - Programme Pack.md`
5. current `README-DEV.md`
6. current `docs/task_packs/MGE Sign-Up Tool.md`
7. current `docs/task_packs/mge_implementation_progress.md`
8. current `docs/reference/deferred_optimisations.md`
9. current `docs/reference/archive/deferred_optimisations_resolved.md`
10. supplied `deferred_optimisations_reconciled_proposal.md`
11. supplied `deferred_optimisations_resolved_append.md`

This is a documentation-only reconciliation.

Locked outcomes:

- Phase 5.2, Phase 8.1 and MGE Process Polish Phase 2 remain archived.
- GovernorOS is complete through Phase 8.1; Phase 9 `/stats kingdom` remains proposed and gated.
- MGE Tasks A-M are complete; Task N becomes `Closure audit pending`, not an untouched broad build.
- Task C Slice 13 remains active but must refresh its July evidence/object map before execution.
- KVK_ALL Phase 4/10 top-level documents are retained test-contract fixtures, not active work.
- Remove `dbo.vAllianceActivity_WeeklyCumulative` and the SQL promotion workflow from the active deferred register and append their supplied resolved-history entries.
- Remap the import-staging deferred item to `dbo.IMPORT_STAGING_PROC_CORE`.
- Add `Status` and `Last verified` after the seven required deferred fields.
- Add the MINI_AMD 5-minute/15-minute transaction-log backup cadence policy item.
- The reconciled active deferred count must be exactly 25.
- Keep `_offload_callable` once-only failure semantics as the highest-priority implementation-ready item.
- Make no Python, SQL, config, command, permission, dependency, asset, cache, scheduler or live schedule change.

Inspect the current worktree before editing and preserve any newer operator changes. Do not rewrite
historical delivery records merely to update old test counts; change only present-tense status or
factually obsolete backlog statements.

Run the exact validation in the task pack. A full runtime pytest, SQL validation, restart, resync and
Discord smoke may be explicitly skipped because this task changes Markdown/archive/index records
only.

Return:

- exact changed-file manifest;
- confirmation that the five archive moves remain intact;
- old-to-new status summary;
- active deferred count and resolved-history additions;
- validation output;
- documented security-review skip;
- any source statement that could not be verified without guessing;
- the recommended next engineering task.
